"use strict"

export default class BPApplication {
    public init(...args: any) {
        /**
         * 将index.ts中的东西转移进来
         */
    }

    /**
     * 创建解析器，分几步呢
     * 1. 首先，需要将窗口抽象成树型结构
     */

}